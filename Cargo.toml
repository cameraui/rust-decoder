[package]
edition = "2021"
name = "cameraui-rust-decoder"
version = "0.0.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "3.12.0", default-features = false, features = ["napi9"] }
napi-derive = "3.6.1"
yuvutils-rs = { version = "0.8.3" }
fast_image_resize = { version = "6.1.0", features = ["rayon"] }
image = "0.25.10"

[build-dependencies]
napi-build = "2.4.0"

[profile.release]
lto = true
strip = "symbols"
